const add = (a, b) => a + b;
const sub = (a, b) => a - b;
const div = (a, b) => a / b;
const mul = (a, b) => a * b;

module.exports = { add, sub, div, mul };

//we can write/export the above whole code like this also :
/*
        exports.const add = (a, b) => a + b;
        exports.const sub = (a, b) => a - b;
        exports.const div = (a, b) => a / b;
        exports.const mul = (a, b) => a * b;
*/
